# Backend environment variables.
# Copy to .env (which is gitignored) and adjust for your local setup.

NODE_ENV=development
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info

# Optional metadata surfaced by GET /api/v1/version. Injected at build/deploy time.
# BUILD_VERSION=
# BUILD_COMMIT=

# Reserved for Phase 04 (PostgreSQL & Drizzle) — not consumed yet.
# DATABASE_URL=postgres://cyberpravda:cyberpravda@localhost:5432/cyberpravda
